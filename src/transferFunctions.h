
#include <math.h>       /* exp */
namespace transferFunctions {
	/*double sigmoid(double x) {
		return 1.0 / (1.0 + exp(-x));
	}
	double sigmoidPrime(double x) {
		double fx = exp(-x);
        return fx / (1 + fx*fx);
	}*/
}
